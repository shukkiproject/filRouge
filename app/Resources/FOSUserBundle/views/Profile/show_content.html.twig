{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
	{% if user.photo %}
        <img src="{{ user.photo }}" alt="" height=300 />
    {% endif %}
    <p>{{ user.username }}</p>

</div>
